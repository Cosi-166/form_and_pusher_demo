<h1>Search Form</h1>
<%= form_tag(search_path, method: "get") do %>
  <%= label_tag(:title, "Title:") %>
  <%= text_field_tag(:title) %>
  <%= label_tag(:desc, "Description:") %>
  <%= text_field_tag(:desc) %>
  <%= label_tag(:title, "Type:") %>
  <%= text_field_tag(:type) %>
  <%= submit_tag("Search Now!") %>
<% end %>
=======
<div class="col-md-offset-1 col-md-4">
  <h1>Search</h1>
    <%= form_tag(do_search_items_path, method: "get", role: "form", class: "form") do %>
      <div class="form-group">
        Items that
        <%= select_tag(:match_no_match, 
              options_for_select([['match', 1], ["don't match", 2]]), class: "form-control") %>
        <%= select_tag(:all_some, 
              options_for_select([['all', 1], ["some", 2]]), class: "form-control") %>        of the following conditions
      </div>
      <div class="form-group">
        <label>Category:</label>
        <%= select_tag(:catgegory_id, options_for_select(@categories), class: "form-control") %>
      </div>
      <div class="form-group">
        <label>Title</label>
        <%= text_field_tag(:title, "", class: "form-control") %>
      </div>
      <p><%= submit_tag("Search") %>
    <% end %>
</div>
